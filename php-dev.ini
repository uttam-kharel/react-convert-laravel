; PHP development overrides for the Livewire app
; Used by the dev server via: php -c php-dev.ini artisan serve
[PHP]
display_errors = On
display_startup_errors = On
error_reporting = E_ALL
log_errors_max_len = 0
assert.exception = On
opcache.enable = 0
